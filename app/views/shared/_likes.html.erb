<% if user_signed_in? && item.liked_by?(current_user) %>
  <%= link_to item_likes_path(item, custom_view: 'other'), method: :delete, remote: true, class: 'like-button' do %>
    <i class="fas fa-thumbs-up me-2" style="color: #393E46;"></i> <%= item.likes.count %>
  <% end %>
<% else %>
  <% if user_signed_in? %>
    <%= link_to item_likes_path(item, custom_view: 'other'), method: :post, remote: true, class: 'like-button' do %>
      <i class="far fa-thumbs-up me-2"></i> <%= item.likes.count %>
    <% end %>
  <% else %>
    <%= link_to new_user_session_path, class: 'like-button' do %>
      <i class="far fa-thumbs-up me-2"></i> <%= item.likes.count %>
    <% end %>
  <% end %>
<% end %>
